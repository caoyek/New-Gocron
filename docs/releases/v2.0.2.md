# New-Gocron v2.0.2

## 更新内容

- 任务列表新增置顶功能，可将任意分页中的任务固定显示在第一页。
- 置顶设置按浏览器和登录用户分别保存，不同浏览器之间互不影响。
- 搜索、标签、任务节点、执行方式和状态筛选仍然有效。

## 升级说明

本次更新不涉及数据库结构变更。停止旧版服务后替换 `gocron` 主程序即可；如需同步节点版本信息，可同时替换 `gocron-node`。请保留原有的 `conf/app.ini`、`conf/install.lock` 和 `conf/.version`。
